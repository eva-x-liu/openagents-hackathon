# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# BookClub Core - 环境变量配置示例
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 
# 使用方法：
# 1. 复制此文件为 .env：cp env.example .env
# 2. 填写实际的配置值
# 3. .env 文件已被 git 忽略，不会泄露密钥
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ========== 必需配置 ==========

# Google Gemini API Key（必需）
# 获取地址：https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# OpenAgents Workspace Secret（推荐配置）
# 用于 Agent 之间的安全通信
OA_WORKSPACE_SECRET=your_workspace_secret_here

# ========== 可选配置 ==========

# 模型选择（默认：gemini-2.0-flash-exp）
GEMINI_MODEL=gemini-2.0-flash-exp

# ========== 性能优化（赠金账户专用） ==========

# PDF 文件复用 ID（首次启动后会自动提示）
# Gemini 上传的文件有 48 小时有效期
# 配置后可节省 10-30 秒启动时间
# 
# 使用方法：
# 1. 首次启动后，查看输出中的 "PDF 上传成功！ID: files/xxxxx"
# 2. 将 File ID 复制到下面这行（取消注释）
# 3. 重启系统时会自动复用
#
# PDF_FILE_REF=files/your_file_id_here

# ========== 其他配置 ==========

# Python 路径（通常不需要修改）
# PYTHONPATH=.
